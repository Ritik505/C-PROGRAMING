#include <stdio.h>
#include <stdlib.h>
int main() {
    int num;
    printf("Enter a number (0 to exit): ");
    scanf("%d",&num);
    if(num==0){
        printf("Exiting the program...\n");
        exit(0);
    }
    printf("You entered: %d\n",num);
    return 0;
}
