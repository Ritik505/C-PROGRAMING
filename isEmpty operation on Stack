#include <stdio.h>
#define SIZE 5

int stack[SIZE],top = -1;

void isEmpty(){
    if(top ==  -1){
        printf("stack is Empty\n");
    }
    else
        printf("Stack is not Empty\n");
}

int main() {
    isEmpty();
    stack[++top] = 10;
    stack[++top] = 20;
    stack[++top] = 30;
    isEmpty();

    return 0;
}
