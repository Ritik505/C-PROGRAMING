#include <stdio.h>
#define SIZE 5

int stack[SIZE],top = -1;

void peek(){
    if(top ==  -1){
        printf("stack is Empty");
    }
    else
        printf("Peek %d\n",stack[top]);
    
}

int main() {
    
    stack[++top] = 10;
    stack[++top] = 20;
    stack[++top] = 30;
    peek();

    return 0;
}
